version: 2

models:
  - name: mrt_products_performance
    description: '{{ doc("mrt_products_performance") }}'
    columns:
      - name: month_year
        description: Primary key, unique identifier for each row.
  
