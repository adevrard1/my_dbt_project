version: 2

models:
  - name: mrt_stores_performance
    description: '{{ doc("mrt_stores_performance") }}'
    columns:
      - name: month_year
        description: Composite primary key with store_id, unique identifier for each row.
      - name: store_id
        description: Composite primary key with month_year, unique identifier for each row.    
  
