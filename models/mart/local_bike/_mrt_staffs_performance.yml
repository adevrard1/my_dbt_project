version: 2

models:
  - name: mrt_staffs_performance
    description: '{{ doc("mrt_staffs_performance") }}'
    columns:
      - name: month_year
        description: Primary key, unique identifier for each row.
  
