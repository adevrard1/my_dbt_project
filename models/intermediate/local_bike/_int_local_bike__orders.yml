version: 2

models:
  - name: int_local_bike__orders
    description: '{{ doc("int_local_bike__orders") }}'
    columns:
      - name: order_id
        description: Composite primary key with product_id, unique identifier for each row.
        tests:
            - not_null  
      - name: product_id
        description: Composite primary key with order_id, unique identifier for each row.  
        tests:
            - not_null  