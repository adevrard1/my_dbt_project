version: 2

models:
  - name: int_local_bike__stocks
    description: '{{ doc("int_local_bike__stocks") }}'
    columns:
      - name: store_id
        description: Primary key, unique identifier for each store.
        tests:
            - unique 
            - not_null  
  
