version: 2

models:
  - name: int_local_bike__products
    description: '{{ doc("int_local_bike__products") }}'
    columns:
      - name: date
        description: Composite primary key with product_id, unique identifier for each row.
        tests:
            - not_null  
      - name: product_id
        description: Composite primary key with date, unique identifier for each row.  
        tests:
            - not_null  